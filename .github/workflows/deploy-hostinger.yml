name: Deploy para Napoleon

on:
  push:
    branches:
      - main # Quando houver push para a branch main, a automação executa

jobs:
  web-deploy:
    name: 🚀 Deploy Napoleon (SFTP)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🚚 Baixando os arquivos
        uses: actions/checkout@v4

      - name: 🛠️ Instalando o Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Instalando dependências
        run: npm ci

      - name: 🏗️ Construindo o projeto
        run: npm run build

      - name: 🔍 Inspecionar diretórios no servidor
        run: |
          sudo apt-get update && sudo apt-get install -y sshpass
          export SSHPASS="${{ secrets.FTP_PASSWORD }}"
          echo -e "pwd\nls -la\nls -la public_html\nls -la public_html/trglogistics.com.br" | sshpass -e sftp -o StrictHostKeyChecking=no -o ConnectTimeout=15 -P 1157 "${{ secrets.FTP_USERNAME }}@${{ secrets.FTP_SERVER }}" > directory_structure.txt 2>&1 || true

      - name: 📤 Upload de artefato
        uses: actions/upload-artifact@v4
        with:
          name: directory-log
          path: directory_structure.txt

      - name: 🚀 Enviando para a Napoleon via SFTP
        uses: milanmk/actions-file-deployer@master
        with:
          remote-host: ${{ secrets.FTP_SERVER }}
          remote-protocol: sftp
          remote-port: 1157
          remote-user: ${{ secrets.FTP_USERNAME }}
          remote-password: ${{ secrets.FTP_PASSWORD }}
          remote-path: /public_html/
          local-path: ./dist/
          sync: full


